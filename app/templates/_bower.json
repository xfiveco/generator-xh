{
  "name": "<%= _.slugify(projectName) %>",
  "version": "0.0.1",
  "ignore": [
    "**/.*",
    "node_modules",
    "bower_components",
    "src/bower_components",
    "test",
    "tests"
  ],
  "dependencies": {<% if (useModernizr) { %>
    "modernizr": "~2.8.1",<% } %><% if (useBootstrap) { %>
    "bootstrap": "~3.1.1",<% } %><% if (cssPreprocessor === 'SCSS' && !useBootstrap) { %>
    "normalize-scss": "~3.0.0",<% } %><% if (cssPreprocessor === 'LESS' && !useBootstrap) { %>
    "normalize.css": "~3.0.1",<% } %>
    "jquery": "~1.11.1"
  },
  "devDependencies": {}
}
